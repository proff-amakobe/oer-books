# Publication-only changes for the original manuscript reset

No file under `original/` was edited, renamed, moved, normalized, or replaced. The new original files are committed exactly as locked, excluding only incidental `.DS_Store` files.

Changes outside original/:

- `_quarto.yml`: explicit direct references to the 15 original chapter QMDs; five external grouping Parts; automatic numbering disabled; prior semantic-code filter and generated bibliography/citeproc behavior disabled.
- `_quarto-html.yml`, `_quarto-print.yml`, `_quarto-epub.yml`: separate `output/reset/` destinations, original-review download filenames, and neutral presentation styles.
- Separate landing page, Preface, references explanation, and author page: concise publication framing based on the supplied project arc and identity. No chapter prose imported from the previous rewrite; no unverified biography added. Web actions are excluded from the print/EPUB body.
- `styles/reset-html.scss`, `reset-epub.css`, `reset-print.tex`: ordinary wrapping, standard verbatim rendering at normal font size, Letter paper, existing authored wrapfigure support, and embedded Unicode glyph support. No terminal transformation, figure creation, table wording changes, or print redesign.
- `scripts/reset-resources.lua`: only the raw-LaTeX image bridge and JPEG resource alias; no heading or code-block handler.
- `scripts/build.py`: hash checks before and after render; generated byte-identical aliases under ignored `assets/images/`; uncommitted font dependencies; cleanup of known Quarto intermediate HTML files outside the locked source set. No rewritten chapter copies.
- `scripts/assemble_web.py`: reset download bundle and canonical web links.
- `scripts/lock_original.py`, `inventory_original.py`, `qa/verify_original.py`: raw-byte integrity, report-only inventory, and complete original-source/rendered-output fidelity checks.
- `.gitattributes`: disables newline conversion for original files. `.gitignore`: excludes generated image aliases and build resources.
- `chapters/README.md`: marks the previous rewrite NOT ACTIVE MANUSCRIPT. Previous phase reports are labeled SUPERSEDED BY ORIGINAL MANUSCRIPT RESET.
- Editorial manifests, review-only queue, integrity report, payload inventory, QA result, and reset report: evidence only; no source corrections applied.
- Shared publication workflow: adds required rendering/QA packages, invokes original-source checks, and publishes `output/reset/html/`. No other publication’s manuscript or build command was changed.

## Publication behavior retained for author review

The original metadata titles and body headings are both preserved. Normal Quarto rendering may repeat a title when both appear in a source file; Chapter 15 also has a distinct metadata title and a second top-level Frontier heading. No authored heading was deleted or rewritten to normalize these differences. Explicit author figure captions remain as written, alongside any ordinary renderer-generated caption treatment.

All source technical classes remain intact. The previously added semantic labels, technical corrections, citations, normalized objectives, and renamed chapters are not applied. The former bibliography remains historical and is not included in this build.

## Tooling and prior artifacts

Quarto writes temporary HTML beside an input during rendering; the build moves leftover generated files outside original/ if a render fails, while never moving a path listed in the author lock. Initial rendering exposed a JPEG file with a PNG filename; only generated aliases and the resource bridge were adjusted.

An initial setup command lacked PyYAML and started an old-profile HTML render before configuration had been replaced. That render was stopped. The earlier HTML publication was restored from the successful pre-reset GitHub Pages artifact (run 35468314834); the earlier PDF and EPUB outputs were not rebuilt or overwritten. No old-profile output was used as manuscript material for this reset.

The in-app browser could not initialize in this environment, so headless Chrome supplied the browser checks. Homebrew could not provide qpdf for the installed Xcode/toolchain; qpdf was installed in an isolated temporary Conda environment. These tooling operations did not change the original directory.
